/* Стили для контейнера счетчиков */
.hud-container {
    position: absolute;
    top: 10px;
    right: 10px;
    flex-direction: column;
    align-items: flex-end;
}

/* Стили для счетчика */
.counter {
    font-size: 14px;
    color: white;
    margin-bottom: 5px;
}
